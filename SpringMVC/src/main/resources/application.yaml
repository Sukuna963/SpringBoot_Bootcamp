spring:
  application:
    name: SpringMVC

  # database configuration
  datasource:
    url: jdbc:mysql://localhost:3306/customers?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create